<!DOCTYPE html>
<html>
<head>
    <title>Manage Departments</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background: #fff8e1; }
        .card { border: none; background: #fff3cd; }
        h2 { color: #e67e22; }
        .btn-orange { background-color: #e67e22; color: white; border: none; }
        .btn-orange:hover { background-color: #ca6f1e; }
    </style>
</head>
<body>
      <!-- Header -->
  <nav class="navbar navbar-expand-lg sticky-top" style="background-color: #0d47a1;">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold text-white" href="/">E-Government Portal</a>
      <div class="d-flex align-items-center">
        <a href="/" class="nav-link text-white me-3">Home</a>
        <a href="/help" class="nav-link text-white me-3">Help</a>
        <form action="/logout" method="POST" class="d-inline">
          <button type="submit" class="btn btn-light btn-sm">Logout</button>
        </form>
      </div>
    </div>
  </nav>
  
<div class="container mt-5">
    <div class="card shadow-lg p-4">
        <h2 class="text-center">Manage Departments</h2>
        <p class="text-center">Here you can manage government departments that provide services.</p>

        <table class="table table-hover mt-3">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Department Name</th>
                    <th>Description</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% if (departments.length > 0) { %>
                    <% departments.forEach(d => { %>
                        <tr>
                            <td><%= d.id %></td>
                            <td><%= d.name %></td>
                            <td><%= d.description %></td>
                            <td>
                                <a href="/admin/departments/edit/<%= d.id %>" class="btn btn-sm btn-warning">Edit</a>
                                <a href="/admin/departments/delete/<%= d.id %>" class="btn btn-sm btn-danger">Delete</a>
                            </td>
                        </tr>
                    <% }) %>
                <% } else { %>
                    <tr><td colspan="4" class="text-center">No departments found.</td></tr>
                <% } %>
            </tbody>
        </table>

        <div class="text-center mt-3">
            <a href="/admin/departments/add" class="btn btn-orange">Add New Department</a>
            <a href="/admin_dashboard" class="btn btn-dark">Back</a>
        </div>
    </div>
</div>
</body>
</html>
